build/
**/build/
__pycache__/
*.pyc
.venv/
venv/
.vscode/
*.log
.claude/
host.log

# Python wheels and other downloaded installers are never project sources.
*.whl
*.tar.gz

# Original camera dump of the board photographs. The renamed, documented set lives in
# documentation/hardware/photos/; keeping both in the repository would double every clone.
Hardwarepics/

# The ground-command password. Never committed; see firmware/flight-computer/include/flight/local_secrets.example.hpp.
firmware/flight-computer/include/flight/local_secrets.hpp
